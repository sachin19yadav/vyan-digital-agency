export const metadata = {
  title: "Admin CRM — Vyan Digital Agency",
  robots: {
    index: false,
    follow: false,
    nocache: true,
  },
};

export default function AdminLayout({ children }) {
  return children;
}
